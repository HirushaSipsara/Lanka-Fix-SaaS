/**
 * Loading.jsx — Loading Spinner Component
 *
 * This file should contain:
 * - A reusable loading indicator component
 * - Props:
 *     - message (optional): text to display below spinner (e.g., "Loading requests...")
 *     - fullPage (optional): if true, centers spinner on full viewport
 * - Visual: spinning animation (CSS or SVG-based)
 *
 * Usage:
 *   <Loading />
 *   <Loading message="Fetching your requests..." />
 *   <Loading fullPage />
 *
 * Export: default Loading component
 */
